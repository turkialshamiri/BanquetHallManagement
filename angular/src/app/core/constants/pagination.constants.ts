/** Default server-side page size for data tables. */
export const DEFAULT_PAGE_SIZE = 6;

/** ABP sorting expression — newest records first. */
export const DEFAULT_SORTING = 'creationTime desc';

/** Used by dialogs and lookups that need a full in-memory list. */
export const DEFAULT_LIST_MAX_RESULT_COUNT = 1000;
